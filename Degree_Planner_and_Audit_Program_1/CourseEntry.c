//
// Created by matth on 1/2/2018.
//

#include <stdio.h>
#include <string.h>
#include "CourseEntry.h"

/**
struct CourseType
{
    char courseName[50];
    char letterGrade[4];
    char creditHours[3];
    //implementation of unique id?
};
typedef struct CourseType Entry;

Entry getEntry(char name, char letterGrade, char creditHours)
{
    Entry newEntry;
    strcpy(newEntry.courseName, name);
    strcpy(newEntry.letterGrade, letterGrade);
    strcpy(newEntry.creditHours, creditHours);
    return newEntry;
}

void printEntry(Entry aEntry)
{
    printf("%s\t%s\t%s\n", aEntry.courseName, aEntry.letterGrade, aEntry.creditHours);
}

*/